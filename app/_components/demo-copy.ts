// The twin's two answers in the RLPF demo. Shared by the animated chat (human
// side) and the static terminal (twin side), so they live in a plain module.
export const A1 = "I don't have enough from you on this yet.";
export const A2 = "Filed. That is your reasoning, not just the fact — I can answer this one myself next time.";
